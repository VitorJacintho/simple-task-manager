import { CreateTaskInteractionDto } from "./create-task-interactions.dto";

describe('CreateTaskInteractionDto', () => {
  it('should be defined', () => {
    expect(new CreateTaskInteractionDto()).toBeDefined();
  });
});
